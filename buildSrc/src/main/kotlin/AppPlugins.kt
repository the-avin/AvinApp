object AppPlugins {
    private const val NAMESPACE = "app_plugins"

    const val COMPOSE = "${NAMESPACE}.compose"
    const val COMPOSE_UI = "${NAMESPACE}.compose_ui"
    const val KOTLIN = "${NAMESPACE}.kotlin"
}